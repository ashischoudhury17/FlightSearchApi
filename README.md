This project is built in .net core8.0 It will run on "http://localhost:5014".
Cors is set up and as angular project runs by default on port 4200, it's set up accordingly.
If the UI project runs on a different port then need to change cors policy and add the port in program.cs page
